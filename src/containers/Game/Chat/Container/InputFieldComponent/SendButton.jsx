import React from 'react';
import PropTypes from 'prop-types';
import Wrapper from './SendButtonStyled';

const sendButton = props => {
  return <Wrapper id="send">send</Wrapper>;
};

sendButton.propTypes = {};

export default sendButton;
